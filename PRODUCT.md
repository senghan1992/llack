# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

사내 팀 구성원. 업무 시간 내내 창을 켜둔 채로 쓰는 사람들입니다 — 채널을 훑고,
스레드에서 결론을 내고, 파일을 주고받고, 팀이 만든 미니앱을 실행합니다.

역할은 네 단계입니다: owner · admin · member · guest. 관리자는 여기에 더해
멤버 초대/제거, 권한 변경, 미니앱 설치·권한 승인을 합니다.

주 사용 상황: 데스크톱 앱(Tauri 창)을 상시 열어둔 상태. 브라우저 탭에서도
같은 UI가 동작하지만 이는 리뷰·검토 경로이고, 일상 사용의 기준은 데스크톱 창입니다.

## Product Purpose

채팅 · 파일 · 팀이 만든 앱을 하나의 창에서 쓰는 사내 협업 도구.

성공은 "하루 종일 켜두고 있어도 피로하지 않고, 필요한 것을 한 번에 찾는 것"입니다.
Slack/Teams 를 기능으로 앞서려는 것이 아니라, 매일 쓰면서 불편했던 지점을
구조적으로 다르게 만드는 것이 목적입니다.

## Positioning

경쟁 제품이 그대로 베낄 수 없는 세 가지 구조적 선택:

1. **스레드가 채널을 가리지 않습니다.** 스레드는 오버레이가 아니라 오른쪽에
   열(column)로 도킹되고, 전사록이 좁아집니다. 미니앱 패널까지 열면 3열이
   나란히 섭니다. 대화 맥락을 잃지 않고 답글을 읽을 수 있습니다.
2. **미니앱이 API 키 없이 동작합니다.** 호스트가 설치 시점에 관리자가 승인한
   스코프로 한정된 단기 토큰을 발급합니다. 앱은 자체 DB도, OAuth 절차도,
   API 키도 갖지 않습니다.
3. **오프라인이 1급 상태입니다.** 로컬 SQLite 캐시에서 즉시 그리고 갱신하며,
   전송은 아웃박스에 쌓여 재시작 후에도 살아남습니다. `client_msg_id` 로
   재전송이 멱등합니다.

## Operating Context

- **하루 8시간 상시 노출.** 장시간 응시 피로가 실제 제약입니다.
- **작업 단위는 채널과 스레드.** 사이드바에서 안 읽음을 훑고, 채널을 열고,
  스레드로 파고들고, ⌘K 로 이동합니다.
- **화면은 대부분 좁혀진 상태로 쓰입니다.** 3열(전사록·스레드·앱)이 동시에
  열리면 전사록 열이 좁아지므로, 좁은 폭에서도 읽히는 것이 기본 조건입니다.
- **읽기 대비 쓰기 비율이 매우 높습니다.** 훑는 동작이 압도적으로 많습니다.

## Capabilities and Constraints

동작하는 것: 이메일/비밀번호 인증(기기별 세션·토큰 회전) · 워크스페이스와 역할 ·
공개/비공개 채널 · 1:1 및 그룹 DM · 마크다운 메시지 · 멘션 · 1단 스레드 · 반응 ·
고정 · 수정/삭제 · 2단계 파일 업로드(선택 · 드래그&드롭) · 이미지 첨부 인라인
미리보기와 라이트박스 · 메시지를 다른 채널/DM 으로 공유(출처 인용) · 컴포저 공유
서식(일정·할 일·결정) · 읽음 상태와 안 읽음/멘션 카운터 ·
알림 수준 · 음소거 · WebSocket 실시간(타이핑·프레즌스·재연결) · 미니앱(매니페스트·
스코프·샌드박스 패널·KV 저장소·봇 계정) · 도크 이름 표시 토글 · 통합 검색(⌘K —
채널·사람·앱·메시지·파일) ·
오프라인 캐시와 아웃박스 · 에이전트 패널(환경설정에서 프로바이더 연결, 연결된 계정의
모델 목록에서 선택).

제약:

- **스레드는 1단**입니다. 답글의 답글은 같은 스레드에 들어갑니다.
- **이모지 반응은 빠른 4개**만 있습니다. 피커가 없습니다.
- **채널 섹션**은 스키마·API 는 있고 편집 UI 가 없습니다.
- **프로필 편집 화면이 없습니다.** API 만 있습니다.
- **검색은 LIKE 스캔**입니다. 교체 지점은 `services/messages.py:search_messages` 한 곳.
- **@here 와 @channel 을 현재 동일하게** 취급합니다.
- UI 는 Tauri webview 와 브라우저 양쪽에서 같은 코드로 돌아갑니다. 셸에 의존하는
  기능(로컬 캐시·아웃박스·키체인·OS 알림·딥링크)은 브라우저에서 빠집니다.

- **레이트 리밋은 인프로세스**입니다(로그인·가입·메시지·검색, 토큰 버킷).
  단일 워커에서 정확하고, 워커가 늘면 한도가 그만큼 완화됩니다.

**미결(설계 미정, 발명하지 말 것):** SSO(SAML/OIDC), 다중 노드 레이트 리밋(Redis),
감사 로그, 보관 정책, 자동 업데이트. 로드맵 1순위이며 아직 없습니다.

## Brand Commitments

- **이름은 `Llack`** 입니다. 유지합니다.
- **Primary color 는 LG Red** 입니다 (`#A50034`). 사용자가 지정한 구속 조건입니다.
- **UI 는 한국어**이며 **존댓말 톤**을 유지합니다. 현재의 정중하고 담백한
  문장(예: "대화의 시작입니다.", "잠시만요…")이 기준입니다.
- **라이트 테마 우선.** 다크는 추후 추가 가능성이 있으므로 토큰 구조는
  두 테마를 견딜 수 있게 잡습니다.

이 제품은 LG 의 공식 제품이 아닙니다. LG Red 는 사내 도구의 팔레트로 지정된
색상일 뿐이며, LG 브랜딩·로고·공식 소속을 주장하는 표현은 넣지 않습니다.

## Evidence on Hand

- **실행 가능한 시드 데이터**: 워크스페이스 "아크메 주식회사", 사람 4명
  (김앨리스/@alice, 이밥/@bob, 박캐롤/@carol, 최데이브/@dave), 채널 5개
  (`#배포` `#개발` `#공지` `#디자인` `🔒경영`) + `#general` `#random`,
  DM 1개, 미니앱 1개("데일리 스탠드업"). `backend/scripts/seed.py`.
- **검증 자산**: pytest 77개, cargo test 50개, 종단 검증 스크립트
  (`backend/scripts/smoke_realtime.py`, `smoke_apps.py`).
- **예제 미니앱**: `examples/apps/standup/` (매니페스트 + 패널 UI + SDK 사용 예).
- **문서**: `docs/ARCHITECTURE.md`, `docs/APP_PLATFORM.md`, `docs/ROADMAP.md`,
  `docs/WINDOWS.md`, `docs/WEB.md`.

없는 것 — **발명 금지**: 실제 고객, 도입 사례, 사용자 수, 벤치마크 수치,
가격, 라이선스, SLA, 수상 이력, 로고 파일(현재 아이콘은 코드로 생성한 자리표시자).

## Product Principles

1. **맥락을 가리지 않는다.** 새 정보는 기존 화면을 덮는 대신 옆에 붙습니다.
   이것이 이 제품의 가장 눈에 보이는 차별점입니다.
2. **훑기가 최우선이다.** 읽기:쓰기 비율이 극단적으로 높습니다. 안 읽음·멘션·
   프레즌스처럼 훑는 정보는 즉시 판별되어야 합니다.
3. **하루 종일 견딘다.** 8시간 노출을 전제로, 대비와 채도는 피로를 기준으로
   정합니다. 강조는 희소할 때만 강조입니다.
4. **권한은 사람의 말로 보여준다.** 미니앱이 요구하는 것을 스코프 문자열이 아니라
   한국어 문장으로 보여줍니다. 승인은 이해한 뒤에 이뤄져야 합니다.
5. **없는 것을 있는 척하지 않는다.** 미완성 영역(SSO, 감사 로그 등)은 문서에
   솔직하게 적혀 있습니다. UI 도 같은 태도를 지킵니다.

## Accessibility & Inclusion

- 본문 텍스트는 WCAG AA(4.5:1) 이상, 큰 텍스트·UI 요소는 3:1 이상.
- **색만으로 정보를 전달하지 않습니다.** 안 읽음·멘션·프레즌스·오류는 색 외에
  형태·굵기·숫자·아이콘 중 하나를 동반합니다.
- 키보드로 완주 가능해야 합니다. ⌘K 팔레트, 채널 이동, 컴포저, 스레드 열기/닫기에
  보이는 포커스 링이 필요합니다.
- 한국어 본문의 줄바꿈과 자간을 전제로 타이포그래피를 정합니다.
