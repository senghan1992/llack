# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

사내 팀 구성원. 업무 시간 내내 창을 켜둔 채로 쓰는 사람들입니다 — 채널을 훑고,
스레드에서 결론을 내고, 파일을 주고받고, 팀이 만든 미니앱을 실행합니다.

역할은 네 단계입니다: owner · admin · member · guest. 관리자는 여기에 더해
멤버 초대/제거, 권한 변경, 미니앱 설치·권한 승인을 합니다.

주 사용 상황: 데스크톱 앱(Tauri 창)을 상시 열어둔 상태. 브라우저 탭에서도
같은 UI가 동작하지만 이는 리뷰·검토 경로이고, 일상 사용의 기준은 데스크톱 창입니다.

## Product Purpose

채팅 · 파일 · 팀이 만든 앱을 하나의 창에서 쓰는 사내 협업 도구.

성공은 "하루 종일 켜두고 있어도 피로하지 않고, 필요한 것을 한 번에 찾는 것"입니다.
Slack/Teams 를 기능으로 앞서려는 것이 아니라, 매일 쓰면서 불편했던 지점을
구조적으로 다르게 만드는 것이 목적입니다.

## Positioning

경쟁 제품이 그대로 베낄 수 없는 세 가지 구조적 선택:

1. **스레드가 채널을 가리지 않습니다.** 스레드는 오버레이가 아니라 오른쪽에
   열(column)로 도킹되고, 전사록이 좁아집니다. 미니앱 패널까지 열면 3열이
   나란히 섭니다. 대화 맥락을 잃지 않고 답글을 읽을 수 있습니다.
2. **미니앱이 API 키 없이 동작합니다.** 호스트가 설치 시점에 관리자가 승인한
   스코프로 한정된 단기 토큰을 발급합니다. 앱은 자체 DB도, OAuth 절차도,
   API 키도 갖지 않습니다.
3. **오프라인이 1급 상태입니다.** 로컬 SQLite 캐시에서 즉시 그리고 갱신하며,
   전송은 아웃박스에 쌓여 재시작 후에도 살아남습니다. `client_msg_id` 로
   재전송이 멱등합니다.

## Operating Context

- **하루 8시간 상시 노출.** 장시간 응시 피로가 실제 제약입니다.
- **작업 단위는 채널과 스레드.** 사이드바에서 안 읽음을 훑고, 채널을 열고,
  스레드로 파고들고, ⌘K 로 이동합니다.
- **화면은 대부분 좁혀진 상태로 쓰입니다.** 3열(전사록·스레드·앱)이 동시에
  열리면 전사록 열이 좁아지므로, 좁은 폭에서도 읽히는 것이 기본 조건입니다.
- **읽기 대비 쓰기 비율이 매우 높습니다.** 훑는 동작이 압도적으로 많습니다.

## Capabilities and Constraints

동작하는 것: 이메일/비밀번호 인증(기기별 세션·토큰 회전, `LLACK_REQUIRE_INVITE`
로 초대 필수 가입 잠금 — 잘못된 초대는 계정 생성 전에 거부) · 프로필·상태 편집,
비밀번호 변경, 로그아웃(환경설정) · 비밀번호 재설정(가입 이메일로 6자리 코드 발송, 15분·5회 제한, 성공 시 전 세션 무효화) · 메일 서버 설정(소유자가 환경설정 → 메일(SMTP)에서 입력·테스트·저장, 비밀번호는 서버에만 남고 화면엔 '저장됨'만; 비어 있으면 환경 변수 → 콘솔 백엔드 순 폴백) · 관리자의 임시 비밀번호 발급(하향 역할만, 세션 전체 무효화) · 구성원 초대(관리자가 링크 발급·대기 목록·회수 →
링크로 가입/로그인하면 자동 수락) · 워크스페이스 없는 계정의 첫 화면(초대 안내 +
워크스페이스 만들기) · 워크스페이스와 역할 ·
공개/비공개 채널 · 채널 둘러보기(사이드바 돋보기 — 공개 채널 목록·인원·주제 → 참여) ·
채널 설정(이름·주제 수정, 구성원 추가/제거, **관리자 위임**(관리자로/관리자 해제), 보관, 나가기 —
채널 머리글의 톱니) · 참여/나감/추가/내보내기/권한 변경은 전사록에 시스템 라인으로 남음(안 읽음
카운트에는 미포함) · 1:1 및 그룹 DM · 마크다운 메시지(굵게·기울임·취소선·인용·링크·글머리·번호
목록·`- [ ]` 체크리스트·제목·파이프 표·코드 블록(복사 버튼)) · 멘션(`@아이디` 와 `@김앨리스` 처럼
**표시 이름 그대로**, `@김앨리스님` 도 인식 — 클라이언트와 서버가 같은 규칙) · 1단 스레드(답글은
채널 안 읽음을 올리지 않음; 부모가 삭제돼도 '답글 N개' 로 도달) · 반응(이모지만 허용) ·
고정(호버 버튼 + 머리글의 고정 목록 → 메시지로 점프) · 수정(Enter 저장)/삭제(2단계 확인) ·
채널별 알림 수준(모두/멘션만/끄기 — '멘션만'은 사이드바 배지·굵기에도 반영) · 2단계 파일
업로드(선택 · 드래그&드롭 · 이미지 클립보드 붙여넣기; 크기 초과 시 한도를 알려줌) · 이미지 첨부 인라인
미리보기와 라이트박스 · 메시지를 다른 채널/DM 으로 공유(앞 6줄 인용 + `<#채널:메시지>` 원문 링크 →
클릭하면 원문으로 점프) · 컴포저 공유 서식(일정·할 일·결정) · 읽음 상태와 안 읽음/멘션 카운터 ·
검색 결과에서 해당 메시지로 점프(페이지백 + 스포트라이트; 스레드 답글은 스레드 열기) · 마지막 보던
채널 복원 · 레이트 리밋에 걸린 전송의 자동 재전송(순서 보존, 무유실) · 파일명 가시성은 다운로드와
같은 규칙(내가 올렸거나 내가 속한 채널에 공유된 것만 목록·검색에 노출) · 다른 기기 모두 로그아웃 ·
브라우저 알림 권한 상태 안내·재요청 · 기존 구성원의 초대 링크 재접속은 오류 없이 진입 ·
파일 모아보기(사이드바 '파일' — 종류·내 파일 필터, 이름 검색, 공유된 메시지로 점프, 이미지는 라이트박스) ·
라이트박스 이전/다음·원본 크기 전환(←/→, 클릭) · 업로드 진행률 · 활동(사이드바 '활동' — 내가 낀 스레드와
새 답글 수, 나를 부른 멘션) · 이모지 피커(반응·컴포저, 한국어/영어 검색, 최근 사용)와 `:tada:` 단축코드
자동완성·전송 시 변환 · 프로필 사진 업로드/제거(256px 정사각, 비인증 immutable URL) · 워크스페이스 구성원
목록·역할 변경·내보내기(관리자) · 로그인 기기 목록·개별 종료 · 링크 앱은 구성원 누구나 추가, 추가 전 서버가
임베드 가능 여부를 확인해(SSRF 가드) 거부 사이트는 '새 탭으로 여는 타일'로, 추가한 사람·관리자가 이름 변경·제거,
변경은 모두 실시간 반영 · 방해 금지 시간·요일과 일시 중지(`/dnd`) · `@here`(접속 중인 사람만)/`@channel` 구분 ·
멘션 배지와 안 읽음 수 병기 · 나중에 보기(저장·메모·리마인더, `/remind`, 사이드바 '나중에') · 첫 진입 안내 카드 ·
사이드바 섹션(채널 묶기·접기) · 초대 메일 자동 발송·재발송 · 링크 카드(og 언펄) · 동영상 인라인 재생(Range·미디어 토큰) ·
슬래시 명령(내장 6종 + 앱) · 인터랙티브 블록(버튼·선택) · 앱 홈 탭 · 관리자: 감사 로그(CSV)·보관 정책(워크스페이스·채널)·
바이러스 검사 상태 · 개발자 콘솔(매니페스트·서명 비밀·API 토큰·웹훅 기록)·앱 심사(서비스 관리자) ·
알림 수준 · 음소거 · WebSocket 실시간(타이핑·프레즌스·재연결) · 미니앱(매니페스트·
스코프·샌드박스 패널·KV 저장소·봇 계정) · 링크 앱(URL 만으로 팀의 웹 도구를
도크에 추가, 메인 패널 iframe 으로 열림 — 브릿지·토큰·스코프 없음, 관리자만
추가) · 도크 이름 표시 토글 · 통합 검색(⌘K —
채널·사람·앱·메시지·파일; 채널명 정확 일치 우선, 검색어 하이라이트, DM 결과는 상대 이름으로 표기) ·
브라우저 알림(숨은 탭, HTTPS) · 오프라인 캐시와 아웃박스 · 환경설정(기능 안내 — 하고 싶은 일→방법 목록 · 에이전트
프로바이더 연결과 연결된 계정의 모델 목록에서 선택).

채널에 하드 삭제는 없습니다. **보관이 이 제품의 삭제**입니다: 목록에서 사라지고
새 메시지를 받지 않지만 기록은 검색에 남습니다. UI 문구도 그렇게 말합니다.

제약:

- **스레드는 1단**입니다. 답글의 답글은 같은 스레드에 들어갑니다.
- **채널 섹션**은 스키마·API 는 있고 편집 UI 가 없습니다.
- **검색은 LIKE 스캔**입니다. 교체 지점은 `services/messages.py:search_messages` 한 곳.
- **@here 와 @channel 을 현재 동일하게** 취급합니다.
- UI 는 Tauri webview 와 브라우저 양쪽에서 같은 코드로 돌아갑니다. 셸에 의존하는
  기능(로컬 캐시·아웃박스·키체인·OS 알림·딥링크)은 브라우저에서 빠집니다.

- **레이트 리밋은 Redis 가 있으면 노드 간 공유**, 없으면 인프로세스입니다(단일 워커에서 정확).

**미결(설계 미정, 발명하지 말 것):** SSO(SAML/OIDC — 사용자가 직접 구현), 데스크톱 자동 업데이트.
그 외 1순위였던 항목(Redis 레이트 리밋·감사 로그·보관 정책·바이러스 검사)은 구현되었습니다.

## Brand Commitments

- **이름은 `Llack`** 입니다. 유지합니다.
- **Primary color 는 LG Red** 입니다 (`#A50034`). 사용자가 지정한 구속 조건입니다.
- **UI 는 한국어**이며 **존댓말 톤**을 유지합니다. 현재의 정중하고 담백한
  문장(예: "대화의 시작입니다.", "잠시만요…")이 기준입니다.
- **라이트 테마 우선.** 다크는 추후 추가 가능성이 있으므로 토큰 구조는
  두 테마를 견딜 수 있게 잡습니다.

이 제품은 LG 의 공식 제품이 아닙니다. LG Red 는 사내 도구의 팔레트로 지정된
색상일 뿐이며, LG 브랜딩·로고·공식 소속을 주장하는 표현은 넣지 않습니다.

## Evidence on Hand

- **실행 가능한 시드 데이터**: 워크스페이스 "아크메 주식회사", 사람 4명
  (김앨리스/@alice, 이밥/@bob, 박캐롤/@carol, 최데이브/@dave), 채널 5개
  (`#배포` `#개발` `#공지` `#디자인` `🔒경영`) + `#general` `#random`,
  DM 1개, 미니앱 1개("데일리 스탠드업"). `backend/scripts/seed.py`.
- **검증 자산**: pytest 165개, cargo test 189개, 헤드리스 브라우저 스모크 92검사
  (`scripts/smoke_ui.py`, `scripts/smoke_flows.py`, `scripts/smoke_team.py` — 5인 팀
  시뮬레이션 회귀, `scripts/smoke_polish.py` — 완성도 배치 회귀, `scripts/smoke_ops.py` —
  운영·사용성·앱 플랫폼 회귀), 부하 테스트(`backend/scripts/loadtest.py`), 종단 검증 스크립트
  (`backend/scripts/smoke_realtime.py`, `smoke_apps.py`).
- **예제 미니앱**: `examples/apps/standup/` (매니페스트 + 패널 UI + SDK 사용 예).
- **문서**: `docs/ARCHITECTURE.md`, `docs/APP_PLATFORM.md`, `docs/DEPLOY.md`, `docs/ROADMAP.md`,
  `docs/WINDOWS.md`, `docs/WEB.md`.

없는 것 — **발명 금지**: 실제 고객, 도입 사례, 사용자 수, 벤치마크 수치,
가격, 라이선스, SLA, 수상 이력, 로고 파일(현재 아이콘은 코드로 생성한 자리표시자).

## Product Principles

1. **맥락을 가리지 않는다.** 새 정보는 기존 화면을 덮는 대신 옆에 붙습니다.
   이것이 이 제품의 가장 눈에 보이는 차별점입니다.
2. **훑기가 최우선이다.** 읽기:쓰기 비율이 극단적으로 높습니다. 안 읽음·멘션·
   프레즌스처럼 훑는 정보는 즉시 판별되어야 합니다.
3. **하루 종일 견딘다.** 8시간 노출을 전제로, 대비와 채도는 피로를 기준으로
   정합니다. 강조는 희소할 때만 강조입니다.
4. **권한은 사람의 말로 보여준다.** 미니앱이 요구하는 것을 스코프 문자열이 아니라
   한국어 문장으로 보여줍니다. 승인은 이해한 뒤에 이뤄져야 합니다.
5. **없는 것을 있는 척하지 않는다.** 미완성 영역(SSO, 감사 로그 등)은 문서에
   솔직하게 적혀 있습니다. UI 도 같은 태도를 지킵니다.

## Accessibility & Inclusion

- 본문 텍스트는 WCAG AA(4.5:1) 이상, 큰 텍스트·UI 요소는 3:1 이상.
- **색만으로 정보를 전달하지 않습니다.** 안 읽음·멘션·프레즌스·오류는 색 외에
  형태·굵기·숫자·아이콘 중 하나를 동반합니다.
- 키보드로 완주 가능해야 합니다. ⌘K 팔레트, 채널 이동, 컴포저, 스레드 열기/닫기에
  보이는 포커스 링이 필요합니다.
- 한국어 본문의 줄바꿈과 자간을 전제로 타이포그래피를 정합니다.
