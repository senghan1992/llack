# ── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ── Node ────────────────────────────────────────────────────────────────
node_modules/
dist/
*.tsbuildinfo

# 데모 빌드 산출물. `make demo` 로 언제든 다시 만들 수 있고, 3MB 짜리 한 파일을
# 커밋하면 리뷰가 불가능한 diff 가 됩니다. 픽스처(src/lib/demo/fixture.json)는
# 소스이므로 커밋합니다.
desktop/dist-demo/
desktop/demo.html
desktop/demo-standalone.html

# ── Rust ────────────────────────────────────────────────────────────────
target/
**/gen/schemas/

# ── Local data ──────────────────────────────────────────────────────────
# The dev database, uploads and logs are all disposable.
backend/var/
*.db
*.db-wal
*.db-shm

# ── Secrets ─────────────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Editors & OS ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Claude Code local settings (per-developer, not shared)
.claude/settings.local.json

# 예제 미니앱 안으로 복사된 SDK 빌드 산출물
examples/apps/standup/vendor/

# 디자인 검증 스크린샷과 세션 상태 (재생성 가능한 산출물)
.impeccable/review/
.impeccable/build/
*.egg-info/
